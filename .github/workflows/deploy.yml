name: Deploy MERN App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/mern-app:latest

      - name: Deploy to CloudStack VM via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          password: ${{ secrets.VM_PASSWORD }} # key এর পরিবর্তে password ব্যবহার করা হয়েছে
          port: 22
          script: |
            # Docker Hub থেকে লেটেস্ট ইমেজটি নামানো
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/mern-app:latest
            
            # আগের কোনো কন্টেইনার থাকলে তা বন্ধ ও রিমুভ করা
            docker stop mern-app || true
            docker rm mern-app || true
            
            # নতুন কন্টেইনার রান করা (আপনার পোর্ট ম্যাপিং অনুযায়ী)
            docker run -d --name mern-app -p 80:3000 --restart always ${{ secrets.DOCKERHUB_USERNAME }}/mern-app:latest